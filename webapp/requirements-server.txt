fastapi>=0.100
uvicorn>=0.23
loguru>=0.6
selenium>=4.16
webdriver_manager>=4.0
undetected-chromedriver>=3.5
# Python 3.12 removed stdlib distutils; undetected-chromedriver 3.5 still
# imports distutils.version.  Setuptools provides the maintained shim.
setuptools>=68
Faker>=18
requests>=2.31
tldextract>=3.2
urllib3>=1.26
# Required by the Selenium driver wrapper and the batch watchdog.  Keeping
# these explicit makes a fresh worker environment behave like the repository
# development environment instead of failing only when the first browser is
# launched.
psutil>=5.9
pyvirtualdisplay>=3.0
