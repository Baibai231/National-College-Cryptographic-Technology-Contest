# 实站诊断脚本

这里的脚本只用于定位某个站点的 DOM、焦点、iframe 和 inline 反馈差异，不属于
正式回归测试。所有脚本必须遵守：只观察和操作密码框，不填邮箱/手机号、不发送
验证码、不点击注册或登录提交、不创建账号。

从项目根目录运行，例如：

```bash
.venv/bin/python tools/diagnostics/baidu_inline.py
```

诊断结论应抽象成通用逻辑并配套自动测试；不得仅凭单站脚本成功宣称全量稳定。
